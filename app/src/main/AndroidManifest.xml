<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="au.kinde">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kinde"
        tools:targetApi="31">

        <meta-data
            android:name="au.kinde.domain"
            android:value="qualitywolves.kinde.com" />

        <meta-data
            android:name="au.kinde.clientId"
            android:value="reg@live" />

        <meta-data
            android:name="au.kinde.clientSecret"
            android:value="NWACRQCdwVGcamUcaAGAYKYB578UQRtYoCikL1mB8m76US3CJxS" />

        <activity
            android:name=".KindeActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>